// Minimal JS for future enhancements
document.addEventListener('DOMContentLoaded', function(){
  // Example: focus the index number field on load
  var idx = document.getElementById('index_number');
  if(idx) idx.focus();
});
